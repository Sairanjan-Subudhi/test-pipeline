echo "pipeline { agent any; stages { stage('Hello') { steps { echo 'Hello, Jenkins!' } } } }" > Jenkinsfile
echo "# Jenkins Pipeline Test" > README.md
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/yourusername/Jenkins-Pipeline-Test.git
git push -u origin main
